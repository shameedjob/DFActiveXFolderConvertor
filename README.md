# DFActiveXFolderConvertor
Converts and upgrades all DF generated ActiveX files using a newly generated package
Optionally, include the version numbers to have it update in all comments in produced files.
# 
# Compilation Instructions

Inside of this directory run:
```
pip install  -r requirements.txt
pyinstaller df-activex-convertor -F
```
